# Pi App — аудит и программа развития

**Дата:** 2026-07-11 · **База аудита:** main @ `9a94efe` (≈13 500 строк: React/TS фронтенд + Rust/Tauri бэкенд)
**Эталон сравнения:** Claude Code Desktop (редизайн апреля 2026, вкладка Code) — [официальный анонс](https://claude.com/blog/claude-code-desktop-redesign), [справочник](https://code.claude.com/docs/en/desktop), [quickstart](https://code.claude.com/docs/en/desktop-quickstart).

Документ отвечает на шесть вопросов пользователя:

1. **UI не отполирован, нет консистенции** → §4.2 (полный список инконсистентностей) и §5.3 (дизайн-система).
2. **Приложение ест 1–3 ГБ RAM** → §4.3 (разбор, откуда память) и §5.2 (план снижения).
3. **Нет «направления» модели / полезного воркфлоу, всё стороннее и триггерится когда захочет** → §2.2 (как это устроено у Claude Code) и §5.1 (план собственного harness) + [SKILLS-AUDIT.md](SKILLS-AUDIT.md).
4. **Покрытие Claude for Mac (Code) частичное** → §3 (gap-анализ по каждой фиче) и §6 (дорожная карта).
5. **Проект нацелен на opensource, но README/инфраструктура/UX не соответствуют; экосистема pi.dev (расширения/скиллы/темы/AGENTS.md/промпты, включая локальные пользовательские) должна быть нативной в фундаменте** → §5.6 (opensource-фундамент) и §5.7 (экосистема как первоклассный объект).
6. **UI держать по мерке самых топовых/красивых/плавных приложений июля 2026** → §5.8 (планка UI: бенчмарки, motion-система, метод контроля).

---

## 0. TL;DR

- **Фундамент хороший.** Архитектура (супервизор pi-процессов, JSONL-фреймер, event-коалесинг, кэши метаданных, browse-режим сессий, git-центр) продумана и покрыта тестами. Ломать её не нужно — нужно достраивать.
- **Память — это в основном не GUI, а дочерние процессы.** Сам WebView укладывается в ~150–250 МБ; 1–3 ГБ набегают из pi-процессов (Node + 20 расширений), dev-серверов превью и — критично — **осиротевших процессов**: `start_kill()` убивает только прямой процесс, а его дети (MCP-серверы, vite под npm) остаются жить. Это баг №1 (§4.1).
- **Главный стратегический разрыв с Claude Code — не панели, а harness.** Claude Code «направляет» модель системным промптом с обязательным воркфлоу (todo-декомпозиция → план → правки → **авто-верификация**), а pi-app полагается на 11 сторонних расширений, которые модель вызывает по настроению. Решение — собственное расширение `pi-app-harness` + режим auto-verify через превью (§5.1).
- **UI-консистенцию чинит не «ещё одна полировка», а дизайн-токены**: сейчас в styles.css 14 произвольных размеров шрифта, 12 радиусов, 2 разных механики закрытия меню и смесь RU/EN в ярлыках (§4.2).
- **Полноэкранный старт** — одна строка `"maximized": true` в tauri.conf.json (исправлено вместе с этим документом).
- **Git приведён в порядок:** ветка `feat/redesign-ui-and-preview` полностью влита в `origin/main`, локальный main обновлён, работа продолжается на main.
- **Opensource сейчас заблокирован формально:** README показывает MIT-бейдж, но файла LICENSE нет (юридически «all rights reserved»), нет `.github/`, CONTRIBUTING, релизов. При этом бэкенд маркетплейса уже умеет все 4 вида пакетов pi.dev (extension/skill/theme/prompt) — UI показывает только два. План: §5.6–5.7.

---

## 1. Что уже сделано хорошо (не сломать при рефакторинге)

Чтобы «программа развития» не превратилась в переписывание, фиксируем сильные стороны текущей базы:

| Область | Что сделано | Где |
|---|---|---|
| Процессы | Лимит агентов + вытеснение самого простаивающего, idle-reaper, PATH login-shell для GUI-запуска | `supervisor.rs` |
| RPC | Строгий JSONL-фреймер (LF-only, CR-strip, юникод-разделители) с тестами | `jsonl.rs` |
| Производительность чата | Коалесинг событий (1 setState / 33 мс), окно рендера 80 сообщений, memo-компаратор MessageView по toolCallIds | `store.ts`, `ChatView.tsx`, `MessageView.tsx` |
| Память фронта | Кап вывода инструментов 60 КБ (голова+хвост), прюнинг >300 выводов до маркера, эвикция фоновых workspace, кэш подсветки ≤80 | `reducer.ts`, `store.ts`, `markdown.ts` |
| Подсветка | Fine-grained shiki core (21 грамматика, ленивая загрузка, только финальные сообщения, кап 20 КБ) | `markdown.ts` |
| Сессии | Чтение активной ветки дерева (leaf→root по parentId), кэш меты по (mtime,size), форк полный/частичный, watcher с дебаунсом | `sessions.rs`, `watcher.rs` |
| Git-центр | Staging/commit/amend, история, чекпоинты через `refs/pi-app/checkpoints/*` (без порчи stash), комментарии к строкам → промпт агенту, PR мульти-провайдерно (gh/glab/web) | `gitops.rs`, `ReviewView.tsx` |
| Конфиги | Атомарная запись с бэкапом, миграция legacy permission-конфигов, пресеты режимов | `config.rs` |
| Browse-режим | Просмотр чужой сессии при занятом агенте: события гейтятся, диалоги переносятся, «Вернуться к активной» | `store.ts` |
| Тесты | cargo ~24 + e2e, vitest 13 — реальные сценарии (форки, ветки git, фреймер) | `src-tauri`, `src/lib/*.test.ts` |

---

## 2. Эталон: Claude Code Desktop (апрель 2026)

### 2.1 Карта фич (вкладка Code)

Источники: [анонс редизайна](https://claude.com/blog/claude-code-desktop-redesign), [справочник Desktop](https://code.claude.com/docs/en/desktop), [quickstart](https://code.claude.com/docs/en/desktop-quickstart), [MacRumors](https://www.macrumors.com/2026/04/15/anthropic-rebuilds-claude-code-desktop-app/).

**Сессии и параллельность**
- Сайдбар со **всеми активными и недавними сессиями**; фильтры по статусу/проекту/окружению, группировка по проекту.
- **Параллельные сессии в одном проекте**: каждая получает изолированную копию через **git worktrees** (`.claude/worktrees/`, настраиваемые каталог и префикс веток, `.worktreeinclude` для gitignored-файлов).
- Split-view двух сессий (Cmd+клик по сессии в сайдбаре), циклирование Ctrl+Tab.
- **Auto-archive** сессии после merge/close её PR.
- **Side chat (⌘;)** — параллельный вопрос с контекстом сессии, не засоряющий основной тред.
- Окружения: **Local / Remote (облако, живёт после закрытия приложения) / SSH** (авто-установка на удалённой машине).
- **Dispatch**: задачи с телефона порождают Code-сессии с бейджем; push-уведомление по завершении.
- «Continue in»: переезд сессии в веб/IDE с переносом контекста.

**Рабочее пространство**
- **Панели drag-and-drop**: chat, diff, browser, terminal, file, plan, tasks, subagent — любая раскладка, ресайз краёв, ⌘\ закрыть панель.
- **Встроенный терминал** (Ctrl+`) в cwd сессии, общее окружение с агентом, вкладки.
- **Файловый редактор**: клик по пути открывает файл, точечные правки, Save, предупреждение о внешних изменениях.
- Правый клик по пути: Attach as context / Open in (VS Code, Cursor, Zed) / Show in Finder / Copy path.
- **Режимы транскрипта** (Ctrl+O): Verbose / Normal / Summary.
- **Usage-кольцо**: контекст сессии + лимиты плана в одном индикаторе.
- Полный список шорткатов ⌘/.

**Диффы и PR**
- Индикатор `+12 −1` открывает **diff view** (список файлов + изменения), «rebuilt for performance on large changesets».
- **Комментарии к строкам** прямо в диффе, пакетная отправка ⌘Enter, Claude правит и показывает новый дифф.
- Кнопка **Review code**: Claude сам ревьюит дифф и оставляет inline-комментарии (только high-signal: ошибки компиляции, явные баги, безопасность).
- **Мониторинг PR**: статус-бар CI (через gh), **Auto-fix** падающих чеков, **Auto-merge** (squash) после зелёных чеков, десктоп-уведомление о завершении CI.

**Превью и браузер**
- **Browser pane** — вкладочный браузер: dev-сервер проекта + HTML/PDF/изображения/видео из проекта + внешние сайты (чистый профиль; классификаторы безопасности + allowlist доменов; Allow once / Always allow).
- **Авто-запуск dev-сервера** после правок; конфиг `.claude/launch.json` (name/runtimeExecutable/runtimeArgs/port/cwd/env/autoPort/program) — **тот же формат, что уже поддерживает pi-app**.
- **`autoVerify` (вкл по умолчанию!): после каждой правки Claude сам делает скриншоты, читает DOM/консоль, кликает, заполняет формы и чинит найденное.** Persist sessions (куки между рестартами сервера).

**Harness-фичи, видимые в UI**
- **Tasks pane**: живой список фоновой работы сессии — сабагенты, фоновые shell-команды, [dynamic workflows](https://code.claude.com/docs/en/workflows); клик — вывод в subagent-панели, можно остановить.
- **Plan pane** — отдельная панель плана.
- Режимы разрешений: Manual / Accept edits / **Plan** / **Auto (классификатор действий)** / Bypass.
- Interrupt & steer: стоп-кнопка или ввод коррекции без остановки текущего действия.
- **Scheduled tasks** (расписания) и **Routines**.
- Уведомление ОС, когда сессия закончила задачу и ты смотришь другую.
- Skills (`/`), плагины (skills+agents+hooks+MCP+LSP) с браузером маркетплейса, **Connectors** (MCP с GUI-настройкой: GitHub, Slack, Linear, Notion…).
- **Computer use** (research preview): управление приложениями/экраном с тирами доступа per-app.
- Compaction: автоматическая при заполнении окна + `/compact`.

### 2.2 Как Claude Code «направляет» модель (harness)

Это ядро жалобы №3 пользователя. Разбор по [анализу системного промпта](https://weaxsey.org/en/articles/2025-10-12/), [Piebald-AI/claude-code-system-prompts](https://github.com/Piebald-AI/claude-code-system-prompts), [Under the hood of Claude Code](https://pierce.dev/notes/under-the-hood-of-claude-code), [how-claude-code-works](https://code.claude.com/docs/en/how-claude-code-works):

1. **Однопоточный агентский цикл** — промпт + история → ответ (текст = конец, tool calls = продолжаем) → результаты инструментов → снова модель.
2. **Принудительная декомпозиция.** Системный промпт **многократно и императивно** требует использовать TodoWrite для любой нетривиальной задачи: разбить на шаги *до* любых действий, помечать шаги in_progress/completed по ходу. Это не «расширение, которое модель может вызвать», а требование верхнего уровня, подкреплённое примерами.
3. **Верификация до объявления успеха.** В todo-список принудительно попадают шаги «прогнать тесты/сборку и починить упавшее»; промпт запрещает объявлять задачу сделанной без проверки. В Desktop это усилено `autoVerify`: после каждой правки агент сам открывает превью, снимает скриншот, читает консоль и чинит.
4. **Explore → Plan → Code → Verify** как рекомендованный цикл (best practices), поддержанный режимом Plan и панелью плана.
5. **Steering как первоклассный механизм**: пользователь印 может печатать коррекции во время работы — агент читает их после текущего действия (у pi это `steer`, pi-app уже отправляет его при стриминге — механика есть, нет культуры/подсказок в UI).
6. **Специализированные сабагенты** (Explore/Plan/Task) и **dynamic workflows** — модель может написать оркестрационный скрипт под конкретную задачу; всё видно в tasks pane.
7. **Контекст-дисциплина**: CLAUDE.md (память проекта), компакция, hooks на события жизненного цикла.

**Вывод:** «постоянный результат» у Claude Code даёт не набор инструментов, а **обязательный контур: декомпозиция → видимый прогресс → автопроверка**, зашитый в системный промпт и продукт. У pi-app все кирпичи есть (rpiv-todo, plannotator, permission-system, превью, чекпоинты), но контур не замкнут: ничего не заставляет модель им пользоваться.

---

## 3. Gap-анализ: pi-app ↔ Claude Code Desktop

Приоритет: 🔴 критично для «замены Claude for Mac», 🟡 важно, 🟢 желательно, ✅ уже есть/лучше.

| Фича Claude Code | Состояние в pi-app | Приоритет |
|---|---|---|
| Обязательный workflow-harness (todo → план → verify) | ❌ Сторонние расширения без принуждения | 🔴 §5.1 |
| Auto-verify правок через превью | ❌ Превью пассивное (iframe), агенту предлагается расширение браузера | 🔴 §5.1 |
| Параллельные сессии **в одном проекте** (worktrees) | ❌ Жёстко 1 агент на workspace (`chats: Record<cwd,…>`) | 🔴 §5.4 |
| Уведомления ОС (агент закончил / ждёт разрешения) | ❌ Нет вообще | 🔴 §5.4 |
| Tasks pane (сабагенты, фоновые команды) | ❌ Нет (pi-subagents ставится, но невидим) | 🟡 §5.4 |
| Встроенный терминал | ❌ Нет | 🟡 §5.4 |
| Файловый редактор (spot edits) | ❌ Только «открыть во внешнем редакторе» | 🟡 §5.4 |
| Drag-and-drop раскладка панелей | ❌ Фиксированная (чат+превью сплит) | 🟢 §5.4 |
| Diff view: комментарии к строкам | ✅ Есть (даже из истории и чекпоинтов) | ✅ |
| Review code (агент ревьюит свой дифф) | ❌ Нет кнопки (промпт руками) | 🟡 §5.1 |
| Мониторинг PR + auto-fix CI + auto-merge | ❌ Только создание PR | 🟡 §5.4 |
| Side chat (⌘;) | ❌ Нет | 🟡 §5.4 |
| Режимы транскрипта Verbose/Normal/Summary | ❌ Один режим | 🟢 |
| Usage-индикатор (контекст + план) | ✅ Контекст-кольцо + токены/стоимость (план не актуален для локалок) | ✅ |
| Permission modes | ✅ Ask/Accept-edits/Plan/Auto/Bypass через pi-permission-system | ✅ |
| Scheduled tasks / routines | ❌ Нет (есть pi-loop/task-scheduler у сообщества) | 🟢 |
| Облачные/SSH-сессии | ❌ Только local | 🟢 (SSH интересен) |
| Skills/плагины/маркетплейс | ✅ Маркетплейс pi.dev (расширения+skills), рекомендации | ✅ |
| Connectors (MCP GUI) | 🟡 Редактор mcp.json есть; нет каталога/1-клик пресетов | 🟡 §5.1 |
| Компакция авто+ручная | ✅ Ядро pi + UI (кольцо, тумблер, кнопка) | ✅ |
| @-упоминания файлов с автокомплитом | ❌ Только drag&drop путей | 🟡 §5.4 |
| Вложения (изображения) | ✅ Paste/drag/диалог + валидация text-only моделей | ✅ |
| Steering/queue во время рана | ✅ steer/follow-up + видимая очередь | ✅ |
| Rewind/checkpoints файлов | ✅ Свои чекпоинты + pi-rewind (глубже, чем у Claude) | ✅ |
| Аналитика (стоимость/heatmap/streaks) | ✅ Стартовый экран (у Claude такого нет) | ✅ |
| Computer use | ❌ (частично закрывается pi-chrome / agent-browser) | 🟢 |
| Menu bar, Quick Entry (⌥Space), диктовка | ❌ Нет (в PLAN.md §5 запланировано) | 🟡 |
| Подписанные релизы + автообновление | 🟡 Самосборка из исходников (работает, но требует dev-окружения) | 🟡 |
| Локальные модели / кастомные провайдеры | ✅ **Сильнее Claude**: models.json, probe эндпоинтов, stall-timeout настройка | ✅ |

Итого: паритет по «чат+git+конфиги+аналитика», критичный разрыв — **harness, параллельность, авто-верификация, уведомления, терминал/редактор**.

---

## 4. Аудит кодовой базы

### 4.1 Баги и дефекты (приоритизированные)

**P0 — влияют на память/стабильность (связаны с жалобой «1–3 ГБ»)**

1. **Осиротевшие дочерние процессы при kill.** `supervisor.rs:305` (`kill_by_id`) и `preview.rs:160` вызывают `child.start_kill()` — SIGKILL получает только прямой потомок. pi спавнит MCP-серверы/Node-хелперы, `npm run dev` спавнит vite → после idle-kill, вытеснения, смены режима разрешений (перезапуск агента!) или «Стоп» превью дети переезжают под launchd и продолжают есть RAM и порты. За день накапливаются сотни МБ«приложения». **Фикс:** спавнить в собственной process group (`pre_exec: setpgid(0,0)` на Unix) и убивать группу `kill(-pgid, SIGTERM)` → таймаут → SIGKILL. Это же чинит «порт занят» при рестарте превью.
2. **Жадный спавн агента.** `ChatView.tsx:1048`: открытие пустого workspace немедленно поднимает pi-процесс (сотни МБ при 20 расширениях) до первого сообщения. PLAN.md §3.2 обещал ленивый спавн. **Фикс:** спавнить в `sendPrompt`/`ensureAgent` только по первому сообщению; для загрузки списка моделей — короткоживущий `pi --list-models` (уже в ALLOWED списке `pi_cli.rs`).
3. **Зависшие RPC-промисы после смерти агента.** `store.ts`: `pending` не отклоняются на `agent-exit` → операции (fork, switch_session…) молча висят до таймаута (до 180с у rewind). **Фикс:** на `agent-exit` реджектить все `pending` с префиксом `agentId:`.

**P1 — функциональные баги**

4. **`GitBar.createPrViaAgent` без catch** (`ChatView.tsx:109`): если агент занят в другой сессии, `sendPrompt` бросает — unhandled rejection, пользователь не видит причину. Обернуть как в Composer (тост).
5. **Удаление живой сессии не убивает агента при расхождении формы пути**: `deleteSessionAction` (`store.ts:899`) сравнивает `ws.sessionPath === path` строго, хотя везде для этого есть `samePath()`. Симптом: удалили сессию — pi продолжает писать в удалённый файл.
6. **Запись settings.json на каждый нажатый символ**: `SettingsView.tsx` GeneralTab (`defaultProvider`, `defaultModel`, `theme`) вызывает `update()` в `onChange` → атомарная запись файла на каждую букву (I/O-шторм + гонка с pi, который тоже пишет settings). Дебаунс 500 мс или commit по blur.
7. **Кэш подсветки с коллизиями**: ключ `theme:lang:len:code.slice(0,80)` (`markdown.ts:87`) — два блока с одинаковым началом и длиной получат один HTML. Использовать полный хэш (djb2 по всей строке — уже есть в `msgPinId`).
8. **`stripAnsi` (`markdown.ts:5`)** — проверить, что в регэкспе действительно литеральный `\x1b` (в файле он невидим); если нет — режется легитимный текст вида `[1m`. Заменить на явный `/\x1b\[[0-9;]*m/g` + тест.
9. **Дедуп эха user-сообщений по тексту** (`reducer.ts:124`): два одинаковых сообщения подряд («да», «да») — второе не отобразится. Дедупить по (текст + отсутствие optimistic-флага), а не по вхождению в последние 6.
10. **Превью: reload по таймеру** (`PreviewView.tsx:124`): iframe перезагружается через фикс. 1200 мс после старта — если dev-сервер поднимается дольше, видим ошибку до ручного reload. Поллить порт (`probe_url` уже есть) до 200/302 и тогда монтировать iframe.
11. **`.pi/extensions/pi-permission-system/{config.json,.pi-app-mode}` затреканы в git** — локальный permission-режим разработчика попал в репозиторий (PLAN §15 добавил `.pi/` в .gitignore, но файлы уже в индексе). `git rm --cached -r .pi/`.
12. **Escape в палитре команд стирает весь ввод** (`ChatView.tsx:483`): ожидание — закрыть палитру, текст оставить.

**P2 — UX-дефекты и мелочи**

13. Дропдауны закрываются по `onMouseLeave` (ModelPicker, ModeSelector, ContextGauge, BranchPicker, PR-меню) — неустойчиво (задел мышью — закрылось); в Sidebar-меню при этом click-outside. Единая механика: click-outside + Escape.
14. GitBar обновляется только по фронту `streaming` (`ChatView.tsx:103`) — внешние правки (руками в редакторе) не отражаются. Подписаться на `sessions-changed`-подобный fs-watcher или лёгкий поллинг при фокусе окна.
15. Контекстное меню сессии/проекта открывается не у курсора (фикс. позиция строки).
16. `Sidebar.tsx:194` — `{groupId ? "›" : "›"}` бессмысленный тернарник.
17. Diff в ReviewView рендерит все строки без виртуализации — дифф на 10к+ строк подвешивает WebView (см. «rebuilt for performance» у Claude). Виртуализация + сворачивание контекста.
18. `stderrLog` агента копится в store (200 строк), но нигде не показывается — либо панель «Логи агента», либо не хранить.
19. `streaks()`/Heatmap считают дни по UTC (`toISOString`) — «текущая серия» ломается вечером; DST может дублировать день. Считать по локальной дате.
20. `⌘3` тумблерит превью, `⌘1/2/4` переключают вьюхи — несогласованная семантика; нет `⌘K`-палитры, нет `⌘/` со списком шорткатов.
21. `useRequestCountdown` по истечении шлёт `cancelled:true`, хотя pi сам авто-резолвит по таймауту — возможен двойной ответ расширению.
22. Заголовок окна/`productName` = «Pi» — конфликтует с самим pi в Dock/переключателе. Переименовать в «Pi App»/собственный бренд.
23. `csp: null` в tauri.conf.json — включить CSP (у нас dangerouslySetInnerHTML + внешние данные из сессий; DOMPurify есть, но defense-in-depth обязателен).

### 4.2 UI-инконсистентности (жалоба №1)

Собрано по `styles.css` (837 строк) и компонентам:

- **Типографика без шкалы:** font-size 10, 10.5, 11, 11.5, 12, 12.5, 13, 13.5, 14, 15, 17, 18, 20, 24 px — 14 значений, задаются точечно. Нужна шкала из 5–6 токенов (`--fs-xs…--fs-xl`).
- **Радиусы без шкалы:** 2.5, 4, 5, 6, 7, 8, 9, 10, 12, 14, 16, 20 px. Нужно 3–4 токена (`--r-s/m/l/full`).
- **Отступы ad-hoc:** padding/margin везде руками (`8px 24px 12px`, `7px 8px 7px 14px`…). Нужна spacing-шкала кратно 4.
- **Смесь языков:** «Ask permissions» / «Accept edits» / «Plan mode» (EN) рядом с «Новая сессия» (RU); «Create PR», «steer »», «затем ↳», «compact», «fetch/pull/push», «amend» вперемешку с русскими подписями. Решение: словарь строк (`src/lib/i18n.ts`) с единой политикой (предлагаю: весь UI RU, термины git/режимов — EN как имена собственные, но одинаково везде).
- **Глифы вместо иконок:** `⌁` (режим), `▾`, `⋯`, `×`, `›`, `+`, `−` — рядом с lucide-иконками. Всё перевести на иконки одного сета/размера.
- **Две механики закрытия оверлеев** (см. баг 13) и **три вида «меню»**: `.menu`, `.dropdown`, `.palette` с разными паддингами/анимациями — унифицировать в один компонент `Popover`.
- **Нативный `<select>`** (thinking level, база чекпоинтов, sort маркетплейса) рядом с кастомными дропдаунами — на macOS выглядят инородно.
- **Кнопки-строки vs кнопки-чипы vs primary** — использование смешано (напр. «compact» — голый hint-текст-кнопка в статусной строке).
- **Тосты в правом нижнем углу перекрывают статусную строку/композер** при длинных текстах; у Claude уведомления системные.
- **Пустые состояния** разной степени проработки: чат — красивый StartScreen, Review/Marketplace — просто строка `muted`.
- **Анимации:** `msg-in` на каждой карточке инструмента (transform на финальном кадре уже чинился — §PLAN 14), но нет единых длительностей/изингов-токенов; `prefers-reduced-motion` учитывается только глобальным отключением.
- **Светлая тема вторична:** `--bg-raised: #ffffff` = `--bg` (карточки сливаются с фоном), тени слишком лёгкие; тёмная явно основная.
- **Traffic-light зона** захардкожена в px с контр-масштабированием — при uiScale ≠ 1 всё ещё бывает наезд топбара (проверить на 0.7/1.6).

### 4.3 Память: откуда 1–3 ГБ (жалоба №2)

Замеряемая модель (проверить на месте через `ps -o rss -g <pgid>`):

| Компонент | Оценка RSS | Комментарий |
|---|---|---|
| WebView (UI) | 150–300 МБ | Окно рендера 80 сообщений держит DOM в норме; большие диффы Review — главный раздуватель (см. баг 17) |
| Tauri/Rust ядро | 20–40 МБ | Кэши меты/аналитики ограничены |
| **pi-процесс × processLimit(2)** | **300–800 МБ каждый** | Node + 20 расширений + MCP-серверы (context7 и т.п. — отдельные node-процессы!) |
| **Dev-сервер превью** | 200–500 МБ | vite/next + node |
| **Сироты после kill** | растёт со временем | Баг P0-1: MCP-дети и vite переживают idle-kill/«Стоп» |
| Итого | 0.7–2+ ГБ | Соответствует наблюдаемым «1–3 ГБ», причём бо́льшая часть — дочерние процессы, а не GUI |

План снижения (§5.2): process-group kill (P0-1), ленивый спавн (P0-2), processLimit=1 по умолчанию для локальных моделей, авто-стоп превью при неактивности, чистка набора расширений (AGENT-ENV.md уже посчитал: 20 → 9–11 глобально, стартовый промпт 40К → ≤15К токенов), монитор «Память агентов» в настройках (RSS по process group + кнопка kill).

### 4.4 Тест-инфраструктура (bug testing)

Есть: cargo-тесты бэкенда (сессии/git/конфиги/фреймер), e2e agent-тест, vitest для reducer/diff. Нет:
- компонентных тестов UI (React Testing Library) — регрессии вроде «кнопка молча не работает» (история с window.confirm) ловились бы;
- smoke-теста упаковки (tauri build + запуск + resolve_pi);
- теста на утечку процессов (спавн→kill→проверка pgid);
- CI (GitHub Actions: fmt+clippy+cargo test+vitest+tsc) — сейчас всё запускается руками.

---

## 5. Программа работ

### 5.1 П1 — Собственный harness: «направление» модели (главное, жалоба №3)

Цель: контур Claude Code (декомпозиция → план → правки → **авто-проверка**) работает **всегда**, а не «когда модель вспомнит», и виден в UI.

1. **Расширение `pi-app-harness`** (наш npm-пакет, ставится приложением автоматически, как permission-пресеты):
   - **Системный промпт-инжект** (≤2К токенов, дисциплина AGENT-ENV.md §4): императивный воркфлоу — «нетривиальная задача ⇒ todo-список до первого действия; помечай выполнение; перед завершением — верификация (тесты/сборка/превью); после компакции — перечитай todo и сформулируй состояние».
   - **Собственный todo-инструмент** (или обёртка над rpiv-todo) с событием `setWidget` — виджет плана уже рендерится в composer-зоне (`widgetbar`); добавить структурированный формат (чекбоксы, текущий шаг) вместо plain-текста.
   - **Verify-хук:** после `tool_execution_end` для edit/write — инъекция напоминания «запусти проверку»; если в проекте есть `.claude/launch.json` — предложение/автозапуск превью + браузерного инструмента.
   - **Режим Auto-verify** (паритет с `autoVerify` Claude): тумблер в превью-панели; при вкл — после каждого хода с правками приложение само (а) перезапускает/пингует dev-сервер, (б) шлёт follow-up агенту «открой ${url}, проверь консоль/DOM, почини найденное» при установленном pi-agent-browser-native. Формат launch.json уже совместим с Claude — поддержать поле `autoVerify`.
   - Конфиг per-workspace: строгость (off / remind / enforce).
2. **Кнопка «Review code»** в Review-вкладке: готовый промпт «отревьюй текущий дифф, только high-signal» → комментарии агента рендерятся в дифф (мы уже умеем комментарии→агенту; добавить обратный поток: парсить структурированный ответ в inline-карточки).
3. **Куратор окружения** (жалоба «всё стоит сторонне»): экран «Оптимизация» в настройках, реализующий AGENT-ENV.md: показать стартовую стоимость промпта (input+cacheRead первого ответа), список расширений с вердиктами (ядро/per-workspace/дубль), кнопки «применить рекомендованный набор» (`pi remove …`, правка skills-каталогов), пресеты MCP (context7, sequential-thinking) в 1 клик.
4. **Дефолты приложения**: при первом запуске предлагать «рекомендуемый профиль pi-app» — 9 core-расширений + harness + permission-пресет Ask + компакция 70–75%.
5. **Скиллы и скилл-роутер (автоподбор под задачу)** — см. [SKILLS-AUDIT.md](SKILLS-AUDIT.md): уровень 0 ✅ (2026-07-11: мусорные 18 скиллов заменены на 7 кураторских, включая 5 собственных workflow-скиллов в [agent-skills/](../agent-skills) — code-review/verify/debug/testing/ship); уровень 1 — keyword-роутер в `sendPrompt` + проактивные чипы скиллов над композером; уровень 2 — роутинг внутри `pi-app-harness` по типу текущей todo-задачи.

Результат: поведение, за которое отвечает приложение, а не удача — и оно одинаково на Claude-моделях и локальном Qwen (AGENTS.md адаптирован под особенности [Qwable-v2](https://huggingface.co/lordx64/Qwable-v2) — см. SKILLS-AUDIT §4).

### 5.2 П2 — Память и процессы (жалоба №2)

1. Process-group spawn/kill в `supervisor.rs`, `preview.rs`, `pi_cli.rs`, `app_update.rs` (P0-1) + graceful SIGTERM→SIGKILL.
2. Ленивый спавн агента (P0-2); `processLimit` по умолчанию 1 при обнаружении локального провайдера (ollama/oMLX в models.json).
3. Реджект pending на agent-exit (P0-3).
4. Виртуализация: чат (virtua/react-virtuoso, PLAN §3.1 это уже предписывал) и дифф Review.
5. Авто-стоп превью-сервера после N минут без активности панели; индикатор «сервер жив» в тулбаре.
6. Панель «Процессы» в настройках: список агентов/серверов, RSS по pgid, uptime, kill. (`list_agents` уже есть — добавить RSS и UI.)
7. Бюджеты из PLAN §9 перевести в CI-smoke (старт < 1с, RAM UI idle < 180 МБ).

### 5.3 П3 — Дизайн-система и полировка (жалоба №1)

1. **Токены** в styles.css: `--fs-*` (5 размеров + line-heights), `--sp-*` (4/8/12/16/24/32), `--r-*` (4/8/12/full), `--dur-*`/`--ease-*`; прогнать все 837 строк через замену на токены.
2. **Компонент Popover/Menu** единый (click-outside + Esc + позиционирование у якоря/курсора, flip при краях) — заменить `.menu/.dropdown/.palette` и все `onMouseLeave`.
3. **Словарь строк** и языковая политика; вычистить смесь RU/EN (баг-лист §4.2).
4. **Иконки**: только lucide, размеры из шкалы (12/14/16), убрать глифы.
5. Светлая тема: поднять `--bg-raised`/тени, проверить контраст WCAG AA.
6. Пустые состояния и скелетоны для Review/Marketplace/Settings.
7. Хоткеи: `⌘K` палитра действий, `⌘/` шпаргалка, пересмотреть `⌘1–4`; список в меню приложения.
8. A11y: focus-ring, aria-лейблы на икон-кнопках, клавиатурная навигация меню.

### 5.4 П4 — Паритетные фичи Claude Code

Порядок по ценности/стоимости:

1. **Уведомления ОС** (дёшево, критично): агент закончил ход/ждёт разрешения/ошибка провайдера, когда окно не в фокусе или смотрим другой workspace. Tauri notification plugin. + Dock badge с числом ожидающих.
2. **Параллельные сессии в проекте через worktrees**: ключ `chats` → `sessionKey` (cwd+sessionId) вместо cwd; «Новая параллельная сессия» создаёт worktree в `.claude/worktrees/<branch>` (совместимо с Claude), агент спавнится в worktree; в сайдбаре — статусы всех сессий (спиннер уже есть). Auto-archive после merge PR (слушаем `git_summary.branch` + gh pr status).
3. **Встроенный терминал**: xterm.js + portable-pty в Rust; cwd сессии, вкладки. (Открывает и путь к «командам руками рядом с агентом».)
4. **Файловая панель**: клик по пути в чате/диффе → CodeMirror 6 (он же нужен для JSON-конфигов по PLAN §6) с Save/предупреждением о внешних изменениях.
5. **Side chat**: отдельный короткоживущий pi-процесс с `--session` копией текущей сессии (fork_session без записи) или read-only контекст + вопрос; UI — правая панель ⌘;.
6. **PR-мониторинг**: поллинг `gh pr checks` после создания PR, статус-бар CI, кнопки Auto-fix (промпт с выводом упавшего чека) и Auto-merge.
7. **Режимы транскрипта** Verbose/Normal/Summary (фильтр рендера timeline — дёшево, у нас уже всё в items).
8. **@-упоминания файлов** с автокомплитом (индекс файлов workspace в Rust).
9. **Panes/layout**: этап 2 — после терминала/файла/side chat перейти на dockable-раскладку (golden-layout / собственная сетка).
10. **Menu bar + Quick Entry (⌥Space) + диктовка** — PLAN §5, после пунктов выше.
11. **Подписанные релизы**: tauri-bundler + notarization, канал обновлений (сейчас самосборка останется fallback'ом).

### 5.5 П5 — Инфраструктура качества

1. GitHub Actions: `cargo fmt --check`, `clippy -D warnings`, `cargo test`, `tsc --noEmit`, `vitest run`, `tauri build` (smoke).
2. React Testing Library на критичные компоненты (Composer, ExtensionUIDock, SessionRow-меню).
3. Тест утечки процессов (spawn → kill → пустой pgid).
4. Issue-теми в GitHub из баг-листа §4.1 (каждый пункт = issue с меткой P0/P1/P2).

### 5.6 П6 — Opensource-фундамент (жалоба №5, часть 1)

**Диагноз.** README продающий, но проект формально и практически закрыт:
- **LICENSE отсутствует**, при этом README уже носит MIT-бейдж со ссылкой на несуществующий файл. Без лицензии вклад и форки юридически невозможны — это блокер №1. *Решение владельца:* MIT (рекомендация — привычна экосистеме pi/npm, минимум трения) или Apache-2.0 (патентная оговорка).
- Нет `.github/` (CI, issue/PR-шаблоны), CONTRIBUTING.md, SECURITY.md, CODE_OF_CONDUCT.md; нет тегов и Releases — установить приложение можно только сборкой из исходников; секции скриншотов в README пустые (есть один).
- Код местами «личный»: самообновление рассчитано на git-клон исходников (пользователю из Releases нужен dmg-канал), комментарии RU (барьер внешним контрибьюторам).

**Программа:**
1. LICENSE (после выбора) + правка бейджа.
2. **README-рефакторинг** (EN — основной, README.ru — зеркало): hero-скриншоты light/dark + GIF стриминга; «Why pi-app» (vs Claude Desktop / pi TUI — наша ниша: локальные модели, открытая экосистема); Install (Releases dmg, позже brew cask); Quickstart; Architecture (процессная модель из PLAN §3.2 диаграммой); ссылки на ROADMAP/SKILLS-AUDIT/AGENT-ENV; Contributing; Acknowledgements (pi.dev).
3. **CONTRIBUTING.md**: dev-setup (npm i → npm run tauri dev), карта модулей (½ страницы), как гонять тесты (cargo/vitest/e2e), conventional commits, чеклист PR (verify обязателен).
4. **.github/**: workflows — CI из §5.5 + release-сборка по тегу (артефакт dmg; пока без подписи — с инструкцией right-click→Open); ISSUE_TEMPLATE (bug/feature); PR-шаблон.
5. Разметить баг-лист §4.1 в issues, часть — `good first issue`; включить Discussions; ROADMAP-ссылка из README.
6. Политика: новые модули и публичные доки — EN; существующие RU-комментарии переводить по мере касания.

### 5.7 П7 — Экосистема pi.dev как фундамент приложения (жалоба №5, часть 2)

**Идея.** pi.dev — открытый маркетплейс: extensions / skills / **themes** / **prompts & AGENTS.md-пресеты** (сформировавшаяся база «идеального harness-2026»), плюс локальные пакеты, которые пользователи делают сами. «Добавить и использовать» должно быть родным жестом приложения, а не вкладкой в настройках.

**Сейчас:** маркетплейс — 2 вкладки в Settings (extensions, skills) с поиском npm и установкой в 1 клик; `packages.rs` **уже поддерживает kinds `theme` и `prompt`** — UI их не показывает; локальные skills — плоский список; свои AGENTS.md/промпты не управляются вовсе; per-project включение пакетов — руками в JSON.

**Целевая модель:**
1. **Раздел «Библиотека» в сайдбаре** (уровень Чат/Review/Настройки — как Extensions в VS Code/Cursor): четыре коллекции — Extensions, Skills, Themes, Prompts/Harness; в каждой — витрина каталога (поиск/сортировка/категории), «Установленные», «Мои (локальные)».
2. **Детальная карточка пакета**: README с npm, версии+changelog, автор/репо/загрузки; для тем — превью палитры. Доверие: предупреждение о postinstall-скриптах, просмотр package.json до установки, pin версии, стрим лога установки (паттерн `useRunPi` уже есть).
3. **Themes нативно**: установка pi-theme синхронизирует TUI **и** палитру приложения (маппинг на токены §5.3); переключатель тем с живым превью.
4. **Prompts / AGENTS.md-пресеты**: перед применением — дифф с текущим AGENTS.md; слоты global/per-workspace; автобэкап и откат (механика бэкапов уже есть). Наш `agent-skills/` + AGENTS.md — первый встроенный пресет.
5. **Harness-профили как объект**: manifest = extensions + skills + MCP + AGENTS.md + permission-режим + компакция → применить одним кликом, сравнить два профиля, экспорт/шаринг (gist/npm). **Онбординг первого запуска = выбор профиля** (Recommended / Minimal / Custom) вместо пустого экрана.
6. **Локальное творчество нативно**: «Создать скилл» (шаблон SKILL.md + skill-creator), «Создать расширение» (скаффолд), редактирование в файловой панели (CodeMirror, общий с §5.4-4); publish-инструкция в npm.
7. **Per-workspace тумблеры** на каждой карточке (Global / этот проект → пишем `<proj>/.pi/settings.json`), и **бейдж стоимости контекста** — сколько токенов описаний пакет добавляет в стартовый промпт (прямой ответ на проблему 40К из AGENT-ENV).
8. **Установка из потока работы**: агент порекомендовал расширение → карточка-предложение в чате с кнопкой Install (подтверждение обязательно); pv-extbar превью — обобщить в этот же паттерн; из ⌘K.
9. **Сессии/шапка**: индикатор активного профиля в topbar чата (клик — быстрое переключение профиля для новой сессии).

Технически дёшево стартовать: вкладки Themes/Prompts поверх готового `packages.rs` + README-fetch; профили — новый manifest-applier в Rust (запись settings/AGENTS с бэкапами уже есть).

### 5.8 П8 — Планка UI: уровень топовых приложений июля 2026 (жалоба №6)

**Бенчмарки-референсы** (проверять себя против них, а не «в вакууме»): **Raycast** — эталон native-feel поверх WebView, есть реверс-инжиниринг с 8 архитектурными тенетами и **75-пунктным ship-аудитом** ([native-feel-skill](https://github.com/yetone/native-feel-skill)) — принять аудит как наш релизный чеклист; **Linear** — motion и работа со списками; **Arc/Dia** — слоистость и glass; **Cursor/Warp** — плотный dev-UX. Тренды 2026: спринговые микровзаимодействия и «Glassmorphism 2.0» — слои blur+saturation+depth ([DesignRush 2026](https://www.designrush.com/best-designs/apps/ui)).

**Обязательные принципы:**
1. **Motion-система**: анимируем только transform/opacity (бюджет 120 Гц ProMotion); спринги (`motion`) с токен-пресетами (`--spring-snappy/-gentle`), а не разнобой длительностей; **все анимации интерруптибельны** (клик во время анимации не ждёт её конца); переходы состояний без layout-прыжков (skeleton держит высоту); `prefers-reduced-motion` уважается на уровне токенов.
2. **Материалы и глубина**: вибранси сайдбара (`tauri-plugin-window-vibrancy` / NSVisualEffectView), 3 уровня elevation с едиными тенями, полупрозрачные поповеры с blur; светлая тема — полноправная (сейчас вторична, §4.2).
3. **Микровзаимодействия**: press-scale ~0.98 на кнопках, hover 120 мс, optimistic UI (pin/archive/rename применяются мгновенно, откат при ошибке), drag с физикой для перестановки сессий/групп.
4. **Отклик**: правило 100 мс — любое действие даёт визуальный ответ мгновенно; всё длинное — прогресс со стримом (паттерн уже есть в install/update — распространить на всё).
5. **Навигация слоями**: ⌘K-палитра как центр (см. §5.3-7); панели/шиты поверх контекста вместо жёсткой смены экранов; Esc закрывает верхний слой предсказуемо.
6. **Стриминг-эстетика**: чанки как есть (без по-символьных твинов — уже верно), мягкая каретка, shimmer-статусы (есть), progressive disclosure карточек инструментов.
7. **Метод контроля качества UI** (без него планка не держится):
   - 75-пунктный ship-аудит native-feel как чеклист перед каждым релизом;
   - **галерея состояний** на базе mock-бэкенда (у нас уже есть полный mock!) — один экран со всеми компонентами во всех состояниях;
   - скриншот-тесты (Playwright: light/dark × масштабы 0.8/1.0/1.3) против регрессий;
   - правило PR: любое изменение UI — с коротким видео/GIF.

**Зависимость:** сначала токены §5.3 (типографика/spacing/радиусы), затем motion-слой — иначе анимации «прибьют» текущий хаос.

### 5.9 П9 — Личное окружение владельца (конфиги `~/.pi`, НЕ дефолты приложения)

Хвост из [AGENT-ENV.md](AGENT-ENV.md) и [SKILLS-AUDIT.md](SKILLS-AUDIT.md) §5 — чистка личного сетапа под Qwable-v2. Это правки локальных конфигов, приложение для других пользователей не трогаем; всё обратимо через `pi install` / бэкапы.

1. **Расширения: 24 → ~11 глобально** (по вердиктам AGENT-ENV §2, обновлённым на текущий список):
   - удалить дубль: `pi remove npm:@aliou/pi-guardrails` (permission-system функциональнее, на нём работают режимы pi-app);
   - перевести в per-workspace (`<proj>/.pi/settings.json → packages`): `pi-web-access`, `@tintinweb/pi-subagents`, `pi-lens`, `@spences10/pi-team-mode`, `@spences10/pi-observability`, `@amaster.ai/pi-task-scheduler`, `@trevonistrevon/pi-loop`, `@mrclrchtr/supi-cache`, `pi-agent-browser-native` (нужен только с превью);
   - оценить новичков по одному с замером: `pi-ponytail`, `pi-simplify`, `@narumitw/pi-goal` — оставлять только с ощутимой пользой («fewest tools wins» для 35B);
   - ядро остаётся: claude-style-tools, permission-system, plannotator, vcc, retry, statusline, rpiv-todo, rpiv-ask-user-question, mcp-adapter, hermes-memory, rewind.
2. **Sub-agents: 15 → 3–5** в `~/.pi/agent/agents/` (описания всех висят в каждом промпте): оставить `codebase-locator`, `codebase-analyzer`, `web-search-researcher` (+ по вкусу `codebase-pattern-finder`); остальные 11 — в архивную папку `agents/_disabled/` (не удалять).
3. **Замер стартового промпта до/после**: новая сессия → первый ответ ассистента → `input + cacheRead` (видно в статусной строке pi-app или в jsonl). Было ~40К, после чистки скиллов уже меньше; **цель ≤15К**. Зафиксировать цифры здесь.
4. **Регулярность**: после каждой установки пакета — повторный замер (до появления бейджа стоимости контекста из §5.7-7, который сделает это автоматическим).
5. **⚠ Инцидент 2026-07-11 — hermes-memory уводит агента в чужой каталог.** Headless-смоук (`pi -a -p` во временной папке) через кросс-проектную память hermes ушёл в `~/GithubControl/pi-app` и перезаписал там `package.json` (восстановлен из git, commit `0e274de`). Выводы: (а) смоуки гонять с изолированной памятью (выключать hermes per-run) или с явным «работай строго в текущем каталоге» в промпте; (б) кандидат: per-workspace изоляция pi-hermes-memory; (в) permission-система в headless не остановила запись вне cwd — учесть в контракте гейтов R2.

### 5.10 П10 — Доработки второй волны (заявлены 2026-07-11, отресерчены по коду)

1. **User-сообщения рендерились как Markdown** — `MessageView` прогонял ввод через `<Markdown>`: «# заметка» превращалась в заголовок. ✅ Исправлено 2026-07-11: плоский текст `white-space: pre-wrap` (паритет с Claude Code). Осталось на R4: лёгкая подсветка @-упоминаний и путей в user-сообщениях (без полного MD).
2. **Bypass permissions не отключает все гейты.** Ресерч: `.env` блокирует НЕ `pi-permission-system` (у него в bypass `yoloMode: true` — корректно), а **`@aliou/pi-guardrails`** — его дефолтное правило `secret-files` (`.env`, `.env.local`, `.env.production`) работает независимо от режимов pi-app. Системное решение (R2, §5.1): режим разрешений приложения — это контракт со **всеми** гейтующими расширениями: карта известных гейтов (permission-system, guardrails, …) + запись их конфигов при смене режима + предупреждение в UI «режим bypass, но активен сторонний гейт X». Личное окружение: guardrails и так дубль → удалить (§5.9-D1).
3. **Ввод-паритет с Claude Code (вложения/контекст).** Сейчас: картинки идут корректными image-блоками RPC (`prompt.images`), drag&drop из Finder вставляет пути текстом, пасте изображений — base64-вложение. Недостаёт (R4, расширить пункт «@-файлы»): (а) **@-меншены файлов** с fuzzy-поиском по репо и чипами в композере; (б) **drop любых файлов**: изображение → image-блок только если модель vision (`model.input`), иначе путь; текст/PDF → чип «файл в контексте» (путь + опц. краткое содержимое), НИКОГДА не base64 в текст; (в) индикатор в композере, как именно уйдёт вложение (vision/путь); (г) вставка скриншота из буфера ⌘V (есть) + превью-лайтбокс.
4. **Хоткеи и взаимодействие с чатом — полный набор** (R4/R6, вместе с ⌘K и ⌘/): навигация ⌘1..9 воркспейсы, ⌘T новая сессия, ⌘W закрыть, ⌘F поиск по чату, ⌘R Review, ⌘E превью, ⌘, настройки; чат: ⌘Enter отправить (Enter=перенос — настраиваемо), Esc стоп/слой, ⇧⌘Enter steer, ↑ редактировать последнее сообщение, ⌘⇧C копировать последний ответ, ⌘L фокус в композер; вкладка-читшит в ⌘/ и синхронизация с нативным меню macOS (R7).
5. **Дубль сообщений при активном `pi-goal`** — ресерч: расширение само шлёт user-prompt'ы (`sendGoalPrompt`/`sendContinuationPrompt` с обёрнутым текстом) → эхо не совпадает с optimistic-элементом → двоится. Общий скоуп «расширения пишут в сессию» (R2): reconciliation по позиции — первое user-эхо после отправки **заменяет** optimistic целиком (текст эха = истина), последующие user-эхо без пары помечаются бейджем «отправлено расширением» вместо маскировки под пользователя; репро-тест с goal-сценарием в reducer.test.
6. **Темы комьюнити pi.dev — полная поддержка** (расширяет §5.7-3): вкладка Themes в «Библиотеке» (kind `pi-theme` уже поддержан бэкендом), превью палитры в карточке, применение = TUI + маппинг на CSS-токены приложения (§5.3); **кастомизация**: редактор темы (цветовые токены с live-превью), «дублировать и править» для любой установленной, экспорт своей темы в пакет pi-theme (манифест + publish-инструкция).
7. **Аватары агентов** (R5): назначаемая картинка/иконка/анимированный GIF/WebP на воркспейс и на harness-профиль; показ в сайдбаре, шапке чата, уведомлениях ОС и переключателе сессий; дефолт — генерируемый identicon из пути; хранение в app-config (путь/встроенный набор), никакого копирования в репо пользователя.
8. **sequential-thinking MCP — вердикт по нативной интеграции.** Это официальный референс-сервер MCP (structured «thought N/M» шаги с ревизиями). Честная оценка: Claude Code его НЕ использует — его качество даёт харнесс (todo/plan/verify); для сильных моделей поверх нашего R2-харнесса он избыточен. Но для **Qwable-v2 и локальных 35B** это дешёвый способ структурировать рассуждение на сложных многошаговых задачах (reasoning унаследован от дистилла и не улучшен). Решение: (а) включить в рекомендуемый профиль (§5.1-4) как **опциональный** пресет «Local reasoning boost», (б) нативный рендер его вызовов в чате — карточка «Мысль 3/7» со сворачиваемой цепочкой вместо сырого JSON, (в) правило в AGENTS.md уже есть («hard multi-step reasoning → sequential-thinking»), автоподсказка через скилл-роутер (§5.1-5) на задачах с формулировками «спланируй/архитектура/сложный рефакторинг», (г) не форсировать на каждый ход — лишние токены и лупы у 35B.

### 5.11 П11 — Третья волна (заявлена 2026-07-11, вечер)

1. **Гиперпараметры, анти-луп и бенчмарк.** Наблюдаемый отказ (сессии `open-webui-search-integration`, ×2 с rewind): на большой реорганизационной задаче модель уходит в карусель `read`/`todo create→pending→completed` без содержательных действий.
   - **Анти-луп в харнессе (жёсткие рамки), приоритет №1:** (а) детектор повторов — одинаковый `tool+args` подряд: 2-й раз → жёсткий стоп-реминдер, 4-й → блокировка вызова через `tool_call → {block:true}`; (б) детектор todo-осцилляции — ≥3 todo-вызова подряд без read/edit/bash между ними → «прекрати обновлять todo, выполни шаг задачи», ≥6 → блок todo до первого содержательного вызова; (в) мягкий бюджет туров на ран с напоминанием о сжатии плана.
   - **Бенчмарк-скрипт** `bench/`: фиксированный набор задач (5–8: багфикс в игрушечном репо, рефактор, многофайловая фича, websearch-задача, длинная задача с ≥2 принудительными компакциями), headless-прогоны `pi -p` c изолированным HOME (урок §5.9-5), метрики: завершение, туры, луп-скор (повторные вызовы), токены, время; JSON-отчёт. **По нему судить** сетку сэмплинга (temp 0.5–0.8 × topP 0.9/0.95 × repetition/presence penalty) и каждое новое правило харнесса — а не «на глаз».
   - **Websearch не используется никогда.** Причины найдены: `pi-web-access` удалён из глобального набора (D1) — инструмента физически нет в большинстве сессий; в AGENTS.md web-инструменты не упомянуты. Решение: вернуть web-access в рекомендуемый набор (лёгкий) или профиль, правило в AGENTS.md («свежие факты/версии/незнакомые API → web-инструменты, не память»), триггер скилл-роутера («найди/погугли/актуальн/последняя версия» → hint), websearch-таск в бенчмарке.
   - **Долгий тест сессий:** задача на 1–2 часа с реальной компакцией ≥2 раза; проверить фактическое пост-компакционное ре-якорение (правило в AGENTS.md есть — надо измерить, работает ли), деградацию после 8К (train seq) и работу vcc.
2. **Инвентарь инструментов Claude Code → паритет в PiCowork** (что есть у меня нативно и что должно быть у pi):

   | Claude Code | PiCowork сейчас | Действие |
   |---|---|---|
   | Read/Write/Edit/Bash | ✅ built-in + claude-style-tools | — |
   | Grep/Glob (структурные) | через bash rg/find | кандидат: нативные grep/glob тулзы (меньше галлюцинаций флагов) |
   | TodoWrite | ✅ rpiv-todo | — |
   | Task (субагенты) | ✅ pi-subagents (per-ws) | вернуть в рекомендуемый профиль для больших задач |
   | WebSearch/WebFetch | ⚠ pi-web-access (снят в D1) | вернуть (см. п.1) |
   | AskUserQuestion | ✅ rpiv-ask-user-question | — |
   | Skill /slash | ✅ skills + палитра | — |
   | Plan mode | ✅ plannotator | — |
   | Hooks (PostToolUse и т.п.) | ✅ события extension API (harness) | — |
   | Компакция/память | ✅ vcc, hermes | hermes: per-workspace изоляция (§5.9-5!) |
   | Checkpoints | ✅ pi-app git-чекпоинты | — |
   | Artifacts/Notebook | ⬜ нет | не приоритет |
3. **Темы → иконка приложения** (расширяет §5.10-6): установка community-темы перекрашивает и иконку приложения (шаблонная иконка + акцент темы), с ручным выбором варианта; runtime-смена через NSApplication dock icon, персист в app-config. R5.
4. **Полностью фоновое автообновление** (сейчас: вручную закрыть + вручную перенести в Applications): канал git-сборки — фоновая пересборка → атомарная подмена `.app` (rsync в staging → swap) → авто-relaunch с восстановлением окна/сессии; канал Releases — Tauri updater с подписью (R7). Прогресс — тихий бейдж, никакого ручного участия.
5. **CI сломан — диагноз поставлен 2026-07-11:** «The job was not started because your account is locked due to a billing issue» — джобы не стартуют вовсе (4с), это блокировка биллинга GitHub-аккаунта. **Действие владельца: github.com/settings/billing — разблокировать.** Сделано мной: workflow переведён с macos-14 (10× минут) на ubuntu-latest (1×, с deps tauri) и **временно выключен** (`gh workflow enable CI` после разблокировки) — письма о failed CI-main прекратятся.
6. **Лёгкий старт + переименование в PiCowork:** README — однокомандный старт (`git clone … && cd … && npm run bootstrap`, где bootstrap = npm ci + cargo-сборка + `tauri build` + перенос в /Applications); переименование: productName/identifier в tauri.conf, package.json, README/докуменация, имя репо (меняет владелец), упоминания в коде. Сделать одним коммитом-переездом в начале R5.
7. **Нативная i18n:** словарь строк (§5.3-3) становится i18n-слоем: EN + RU полностью (выбор в настройках, авто по локали ОС), архитектурно готово к zh/hi/es/fr/de/pt/ar (после основного). R6, вместе с дизайн-токенами.
8. **Мёртвые кликабельные элементы:** аудит всех интерактивных элементов — каждый либо работает, либо `disabled` с тултипом «в разработке»; автотест «no dead buttons» (playwright кликает все role=button на mock и ждёт видимой реакции/disabled). В чеклист тестирования R6.

---

## 6. Дорожная карта по итерациям

| Итерация | Содержание | Выход |
|---|---|---|
| **R1 «Стоп-утечки»** (~1 нед) | P0-1..3, баги P1 4–12, `maximized` ✅ (сделано), `.pi/` из индекса ✅ (сделано), уведомления ОС (базовые), CI | Память стабильна, «тихих» отказов нет |
| **R2 «Harness»** (~2–3 нед) | §5.1 целиком: pi-app-harness, auto-verify, Review code, куратор окружения, дефолтный профиль | Модель ведома всегда; стартовый промпт ≤15К |
| **R3 «Скорость и вес»** (~1–2 нед) | Виртуализация чата/диффа, процесс-панель с RSS, авто-стоп превью, ленивые чанки | Бюджеты PLAN §9 зелёные |
| **R4 «Паритет»** (~3–4 нед) | Worktrees-параллельность, терминал, файловая панель, side chat, PR-мониторинг, view modes, @-файлы | Закрыты 🔴/🟡 из §3 |
| **R5 «Экосистема и Opensource»** (~2–3 нед) | §5.6: LICENSE, README-рефакторинг, CONTRIBUTING, .github+CI-release, issues из баг-листа; §5.7: раздел «Библиотека» (4 вида пакетов, включая themes/prompts поверх готового packages.rs), карточки с README/changelog, harness-профили + онбординг, per-workspace тумблеры, бейдж стоимости контекста | Проект открыт для вклада; экосистема pi.dev — родной жест |
| **R6 «Дизайн-система и UI-планка»** (~2–3 нед) | §5.3: токены, Popover, словарь строк, иконки, светлая тема, a11y, ⌘K/⌘/; §5.8: motion-система (спринги, интерруптибельность), вибранси, микровзаимодействия, галерея состояний, скриншот-тесты, ship-аудит native-feel | UI держит планку Raycast/Linear; регрессии ловятся автоматически |
| **R7 «macOS-нативность»** (~2 нед) | Menu bar, Quick Entry (⌥Space), диктовка, dock badge, подписанные/нотаризованные релизы | Полный паритет-плюс |

R1→R2 менять местами нельзя (harness бессмыслен, пока агенты текут и падают молча). §5.6 (LICENSE/README) можно сделать раньше любым моментом — это не код. Внутри R6 порядок жёсткий: сначала токены §5.3, потом motion §5.8. R5 и R4 могут идти параллельно (почти не пересекаются по файлам).

### 6.1 R1 «Стоп-утечки» — детальное разбиение (исполняется)

**Цель A — процессы не текут (P0):**
- [x] A1. Групповое завершение процессов: спавн pi/dev-серверов в собственной process group (`setpgid`), kill — SIGTERM группе → грейс → SIGKILL группе (`supervisor.rs`, `preview.rs`); чинит сирот MCP/vite и «порт занят». *(2026-07-11)*
- [x] A2. Убийство всех групп при выходе приложения (`RunEvent::Exit` в `lib.rs`). *(2026-07-11)*
- [x] A3. Ленивый спавн агента: убрать eager `ensureAgent` при открытии workspace; модели/команды подгружаются после реального спавна. *(2026-07-11)*
- [x] A4. Реджект зависших RPC-промисов при `agent-exit`. *(2026-07-11)*

**Цель B — тихие отказы устранены (P1):**
- [x] B1. «Create PR» через агента: catch + тост вместо unhandled rejection. *(2026-07-11)*
- [x] B2. `deleteSessionAction`: сравнение сессий через `samePath` (агент живой сессии гасится корректно). *(2026-07-11)*
- [x] B3. Настройки: debounce записи settings.json/app-config (не писать файл на каждую букву). *(2026-07-11)*
- [x] B4. Кэш подсветки: полный хэш вместо `len+slice(0,80)` (коллизии). *(2026-07-11)*
- [x] B5. Дедуп эха user-сообщений через флаг optimistic (два одинаковых сообщения подряд не теряются). *(2026-07-11)*
- [x] B6. Превью: iframe монтируется по готовности порта (poll `probe_url`), не по таймеру 1.2с. *(2026-07-11)*
- [x] B7. Escape в палитре команд закрывает палитру, не стирая ввод. *(2026-07-11)*
- [x] B8. `stripAnsi` — проверен: ESC-байт в регэкспе есть, ложная тревога (§4.1-8 снят). *(2026-07-11)*

**Цель C — инфраструктура:**
- [x] C1. CI: GitHub Actions (cargo test + tsc + vitest) на push/PR. *(2026-07-11)*
- [x] C2. LICENSE (MIT) + рабочий бейдж. *(2026-07-11)*
- [x] C3. Уведомления ОС: tauri-plugin-notification; агент закончил ход / ждёт разрешения, когда окно не в фокусе или открыт другой workspace. *(2026-07-11)*
- [x] C4. Открытые пункты плана размечены в GitHub issues [#10–#19](https://github.com/NickLitwinow/pi-app/issues) (E2/E4–E8, R2/R3/R5/R6); закрытые P0/P1 из §4.1 в issues не дублируются. *(2026-07-11)*

**Цель D — личное окружение (§5.9, вне кода приложения):**
- [x] D1. Расширения 24→14 (убраны: guardrails-дубль, web-access, subagents, lens, team-mode, observability, task-scheduler, loop, supi-cache, browser-native; бэкап `settings.json.bak-20260711-d1`). Новички ponytail/simplify/goal оставлены — оценивать по одному. *(2026-07-11)*
- [x] D2. Sub-agents 15→4 (codebase-locator/-analyzer/-pattern-finder, web-search-researcher; остальные 11 в `agents/_disabled/`). *(2026-07-11)*
- [x] D3. Замер (pi -p в чистом tmp-каталоге, первый ответ): **35 867 → 13 062 токена (−64%), цель ≤15К достигнута**. Метод: `pi -p "…" </dev/null` (без `</dev/null` pi ждёт stdin — это не зависание). *(2026-07-11)*
- [x] D4. models.json/settings.json: id модели обновлён на `Qwable-v2-DWQ4e-mtp` (oMLX сменил имена; старый id ломал все раны). *(2026-07-11)*

**Цель E — вторая волна (§5.10):**
- [x] E1. User-сообщения — плоский текст вместо Markdown-рендера. *(2026-07-11)*
- [x] E2. Режим разрешений управляет всеми гейтами (§5.10-2): bypass пишет project-local override `.pi/extensions/guardrails.json` (`{"enabled": false}`), выход из bypass снимает его; пользовательский конфиг не трогается никогда (warning-тост); guardrails ищется в глобальных и проектных packages. Тест `gate_contract_bypass_toggles_guardrails`. *(2026-07-11)*
- [x] E3. Reconciliation эха по позиции + бейдж «⚙ отправлено расширением»; тесты: goal-сценарий (переписанный prompt), continuation без пары, два одинаковых подряд. *(2026-07-11)*
- [ ] E4. Вложения-паритет: @-меншены, vision-aware drop, чипы контекста (§5.10-3, R4).
- [ ] E5. Полный набор хоткеев + читшит ⌘/ (§5.10-4, R4/R6).
- [ ] E6. Темы: вкладка Themes, редактор-кастомизация, экспорт пакета (§5.10-6, R5).
- [ ] E7. Аватары агентов/профилей, identicon-дефолт (§5.10-7, R5).
- [ ] E8. sequential-thinking: пресет «Local reasoning boost» + нативный рендер цепочки (§5.10-8, R2/R5).

**Цель F — R2 «Harness» (в работе, issue #16):**
- [x] F1. **pi-app-harness MVP** ([harness-extension/](../harness-extension)): расширение pi с тремя поведениями через ЭФЕМЕРНЫЕ system-reminder'ы (событие `context`, в сессию не пишутся): todo-first на нетривиальный промпт, verify-before-done после edit/write до первой команды (макс 2 раза/ран), скилл-роутер уровня 2 (триггеры → «прочитай SKILL.md»). Проверено смоуками на живом pi: загрузка, матч, инжекция, эфемерность (0 следов в jsonl), нет ложных срабатываний. Установлен глобально (`pi install <путь>`; откат: `pi remove`). Выключатель `PI_APP_HARNESS=0`, отладка `PI_APP_HARNESS_LOG=1` → `.pi/harness.log`. Грабли зафиксированы: project-local пакеты в headless требуют `pi -a`; `\b` в JS-регэкспах не работает с кириллицей. *(2026-07-11)*
- [x] F2. Auto-verify сильнее: надж снимает только успешная команда-проверка (test/lint/build/tsc/cargo/pytest… — `isVerifyCommand`), не «любой bash»; команда берётся из `tool_execution_start` по toolCallId. Смоуки: `echo done` НЕ снимает, `npm test` снимает (`verified by:` в логе). Интеграция с чекпойнтами — позже. *(2026-07-11)*
- [x] F3. Кнопка «Ревью агентом» в топбаре Review (видна при наличии изменений) → канонический промпт по скиллу code-review (только отчёт, без правок), переключение в чат, ошибки — тостом. *(2026-07-11)*
- [x] F4. Куратор окружения: контракт гейтов ✅ (E2), UI-куратор ✅ — баннер «N пакетов · M вне рекомендуемого ядра» во вкладке Расширения + бейджи ≈токенов описаний скиллов (per-skill и суммарно). Онбординг «рекомендуемый профиль» — в R5 (§5.7-5). *(2026-07-11)*
- [x] F5. Роутинг по активной todo-задаче: harness парсит резалт rpiv-todo (структурно `details.params` + оба текстовых формата), матчит subject in_progress-задачи на скиллы, хинт один раз на скилл за ран. Смоук: задача «исправить баг в парсере дат» из файла (промпт без триггеров) → `nudge: skill debug [the active todo]`. Пресет «Local reasoning boost» (E8) — в R5 вместе с профилями. *(2026-07-11)*

**R2 «Harness» закрыт** (F1–F5): модель ведома приложением — todo-first, verify-гейт, скилл-роутер по промпту и по активной задаче, контракт разрешений со сторонними гейтами, куратор окружения. Остатки (онбординг-профили, reasoning-boost) переехали в R5-экосистему.

**Цель G — R3 «Скорость и вес» (в работе, issue #17):**
- [x] G2. Процесс-панель: вкладка «Процессы» в настройках — RSS по process group (агент pi + его MCP-дети; dev-серверы npm+vite; сам процесс приложения) одним вызовом `ps -axo pid,pgid,rss`, автообновление 3с, кнопки «Стоп» (kill_agent/preview_stop). Тест парсера `parse_ps_sums_groups_and_pids`; проверено в превью. WebKit-хелперы macOS вне групп — честно указано в подсказке. *(2026-07-11)*
- [ ] G1. Виртуализация: чат уже оконный («показать предыдущие»); остаётся дифф в Review (сворачивать большие файлы/ханки по умолчанию).
- [ ] G3. Авто-стоп dev-сервера превью после N минут без открытой панели.
- [ ] G4. Ленивые чанки бандла (shiki уже lazy; проверить остальное по vite build --report).

**Цель H — Harness hardening / анти-луп (§5.11-1, приоритет по инциденту open-webui-search-integration):**
- [x] H1. Детектор повторов tool+args: 2-й идентичный подряд → жёсткий стоп-реминдер, 4-й → блокировка (`tool_call → block`). Живая проверка на Qwable-v2: обычный ран — модель послушалась реминдера на x2 (блок не понадобился); стресс-ран «игнорируй предупреждения» — `BLOCK repeat x4`, ран завершился корректно. *(2026-07-11)*
- [x] H2. Детектор todo-осцилляции: ≥3 todo подряд без read/edit/bash → реминдер, ≥6 → блок todo до содержательного вызова (та же механика, что H1). *(2026-07-11)*
- [x] H3. `bench/` создан и прогнан: run.mjs (headless pi, kill группы по таймауту, метрики из jsonl+harness.log) + 5 задач. Бейслайн: 4/4 кодинг-задачи решаются (37–57с, thinking=minimal), websearch ❌ до H4. Бенч сразу окупился — найдено и закрыто два дефекта: (а) блокировки без остановки генерации → **abort-предохранитель** (≥8 блоков → ctx.abort(), подтверждён вживую); (б) **формат-дрейф**: 60×write с пустыми arguments невидимы для tool_call-гардов → **malformed-детектор** на message_end (2 → нудж с форматом полей, 6 → abort). Осталось в H3+: сетка temp×topP×penalty по бенчу (после H4, чтобы websearch-таск был содержательным). *(2026-07-11)*
- [ ] H4. Вернуть websearch: web-access в рекомендуемый набор, правило в AGENTS.md, триггер роутера «найди/актуальная версия».
- [ ] H5. Долгий тест (1–2 ч, ≥2 компакции): замер пост-компакционного ре-якорения и деградации за train seq 8К.

---

## 7. Приложение A: политики принятия решений

- **Не переписывать движок** (супервизор/reducer/фреймер) — только точечные фиксы из §4.1; крупные фичи строить рядом.
- **Совместимость с форматами Claude Code там, где она бесплатна**: `.claude/launch.json` (уже), `.claude/worktrees/`, `autoVerify` — чтобы проекты работали в обоих инструментах.
- **Всё «умное» — в расширение pi (`pi-app-harness`), не в Rust/UI**: harness должен работать и из pi TUI, приложение лишь включает его и рисует виджеты. Это страховка от изменений RPC.
- **Каждая итерация заканчивается на main** (мержи веток — как в этот раз, без длинных расхождений).

## 8. Источники

- [Redesigning Claude Code on desktop for parallel agents — claude.com/blog](https://claude.com/blog/claude-code-desktop-redesign)
- [Desktop application — code.claude.com/docs/en/desktop](https://code.claude.com/docs/en/desktop)
- [Get started with the desktop app — code.claude.com/docs/en/desktop-quickstart](https://code.claude.com/docs/en/desktop-quickstart)
- [How Claude Code works — code.claude.com/docs/en/how-claude-code-works](https://code.claude.com/docs/en/how-claude-code-works)
- [Anthropic Rebuilds Claude Code Desktop App Around Parallel Sessions — MacRumors](https://www.macrumors.com/2026/04/15/anthropic-rebuilds-claude-code-desktop-app/)
- [A Brief Analysis of Claude Code's Execution and Prompts — weaxsey.org](https://weaxsey.org/en/articles/2025-10-12/)
- [Under the hood of Claude Code — pierce.dev](https://pierce.dev/notes/under-the-hood-of-claude-code)
- [Piebald-AI/claude-code-system-prompts — GitHub](https://github.com/Piebald-AI/claude-code-system-prompts)
- [Claude Code Dynamic Workflows — lushbinary.com](https://lushbinary.com/blog/claude-code-harness-every-task-dynamic-workflows-guide/)
- [native-feel-skill (реверс-инжиниринг Raycast: 8 тенетов, 75-пунктный ship-аудит) — GitHub](https://github.com/yetone/native-feel-skill)
- [Best UI App Designs of 2026 — DesignRush](https://www.designrush.com/best-designs/apps/ui)
- Внутренние: [PLAN.md](../PLAN.md) (исходный план и выполненные итерации), [docs/AGENT-ENV.md](AGENT-ENV.md) (аудит окружения pi: 40К стартовый промпт, вердикты по 20 расширениям).
